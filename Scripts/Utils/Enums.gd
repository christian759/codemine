class_name Enums

enum GameState {
	IDLE,
	PLAYING,
	PAUSED,
	GAME_OVER_WIN,
	GAME_OVER_LOSS
}

enum GameMode {
	CLASSIC,
	TIMED,
	SURVIVAL,
	LIMITED_VISION
}

enum TileState {
	HIDDEN,
	REVEALED,
	FLAGGED
}
